{
  "dependencies": {
    "three": "^0.177.0",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
   "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  }
}
